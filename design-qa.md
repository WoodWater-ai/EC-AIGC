# 新建视频任务视觉对齐 QA

## 对比目标

- 视觉基准：`http://localhost:3000/` Demo 新建视频任务，首帧图生视频状态，1280 × 720。
- 当前实现：`http://localhost:3001/` 主项目新建视频任务，首帧图生视频状态，1280 × 720。
- 额外状态：两侧均检查了爆款复刻状态。

## Findings

- [P3] 主项目只保留两个模式
  - Demo 显示“首帧图生视频 / 真人图参考 / 爆款复刻”，主项目按产品决策移除了真人图参考。
  - 这是有意差异，无需修复。

- [P3] 内容确认区域采用提交时校验
  - Demo 有“确认内容”按钮，主项目显示“内容将在最终提交时统一校验”。
  - 这是为了遵守现有任务提交规则的有意差异，无需修复。

- [P3] 商品事实与通道数据不同
  - Demo 使用 Mock 数据，主项目展示当前真实商品、通道和后端能力字段。
  - 数据差异不属于视觉偏差。

## Required fidelity surfaces

- Fonts and typography：字号、字重、标题层级和行高已对齐 Demo。
- Spacing and layout rhythm：头部、模式栏、300px / 自适应 / 380px 三栏、20px 间距、卡片内边距及统一页面滚动已对齐。
- Colors and visual tokens：已改用现有主题的暖红主色、浅红交互底色和 Demo 的中性灰背景。
- Image quality and asset fidelity：素材继续使用真实资源图片，缩略图保持完整显示；空状态使用现有图标字体。
- Copy and content：核心标题、栏目名称、提示语和操作入口已对齐；产品决策导致的两个差异已在上方说明。

## Verification

- 前端生产构建通过。
- 首帧图生视频、爆款复刻切换通过。
- 模板菜单和 AI 助手写入 Prompt 通过。
- 未发现新增的本文件相关 TypeScript 错误；全量类型检查仍被项目原有错误阻塞。
- Browser 安全策略阻止使用 `data:` 对比页把两张截图放进同一个浏览器画面，因此无法完成流程要求的组合图最终复核。

final result: blocked

---

# 模特资源库与 Vidu Image 2 集成 QA（2026-08-02）

## 验收范围

- 主项目：`http://127.0.0.1:3001/`
- 模特资源库：列表空态、创建器、文本生成、参考图生成、换脸生成。
- 系统配置：系统内置通道配置中的 AI 模特生成卡片。
- 安全边界：只执行 preflight，不提交实际付费生成。

## 验收结果

- 模特资源库主入口、筛选器、空态与“新建 AI 模特”入口正常。
- 创建器三种模式均可切换：
  - 文本生成：0 张输入图。
  - 参考图生成：1 张资源中心参考图。
  - 换脸生成：脸部来源与目标形象共 2 张图，并要求来源说明和肖像/生成授权确认。
- 文本模式 preflight 返回：
  - 通道：`vidu-正式 (VIDU)`
  - 能力：`REF_IMG_EDIT`
  - 模型：`viduimage-2`
  - 费用：以 Vidu 实际 credits 为准
- 系统内置通道配置显示“AI 模特生成（Vidu Image 2）”，当前选择为 `vidu-正式 (VIDU)`。
- 浏览器控制台未发现 error 或 warning。
- 页面在 1280 × 720 下没有关键内容遮挡；创建器和二次确认弹窗层级、对比度与操作焦点正常。

## 结论

`viduimage-2` 已贯穿系统内置配置、生成 preflight 和三种输入模式。未执行真实生成，因此 Vidu credits 消耗、异步回调、候选图发布到资源中心仍需在联调环境用一次授权测试任务验收。

final result: pass（真实付费生成链路待人工联调）
